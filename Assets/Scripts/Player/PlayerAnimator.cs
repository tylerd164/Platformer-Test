using UnityEngine;
using static States;

public class PlayerAnimator : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //animator.SetBool("IsJumping", currentState.HasFlag(PlayerStates.Jumping));
        //animator.SetBool("IsWalking", currentState.HasFlag(PlayerStates.Walking));

    }
}
